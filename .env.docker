# Configuración Docker PostgreSQL + PostGIS UTF-8 Nativo
# Reemplaza problemas de encoding Windows y Oracle Cloud

# Conexión Docker PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=citrino
DB_USER=citrino_app
DB_PASSWORD=citrino123
DB_SSLMODE=prefer

# Encoding UTF-8 nativo (no más problemas Windows)
PYTHONIOENCODING=utf-8
LANG=es_ES.UTF-8
LC_ALL=es_ES.UTF-8

# Configuración API
FLASK_ENV=development
API_PORT=5001
API_HOST=0.0.0.0

# PostGIS completo (funcional, no como Oracle Cloud)
POSTGIS_VERSION=3.3
SPATIAL_INDEXES=true